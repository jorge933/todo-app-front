import { ShowInputErrorsDirective } from './show-input-errors.directive';

describe('ShowInputErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInputErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
